# 更新日志

本项目版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)（SemVer）。
版本号唯一来源为 `pomodoro/__init__.py` 中的 `__version__`。

## [2.3.3] - 2026-09-18

### 修复
- 卸载时 `[Code]` 使用了 Inno Setup 不存在的常量 `{userprofile}`，触发 “Unknown constant” 运行时错误；改用环境变量常量 `{%USERPROFILE}`

## [2.3.2] - 2026-09-18

### 修复
- 卸载时程序若仍在运行（托盘 / 悬浮球），文件被占用会导致卸载不完整、Uninstall 注册表键残留；现通过 `AppMutex` 检测并要求先关闭程序

### 变更
- 卸载时弹窗询问是否一并删除本地配置与统计数据（`%USERPROFILE%\.pomodoro_timer`），默认保留

## [2.3.1] - 2026-09-18

### 修复
- 启动后首次收起为悬浮球时显示 25:00，而非实际剩余 / 已用时间
- 长休息按历史累计番茄数取模判断，导致本次运行的第一个番茄可能直接进入长休息；改为按本次运行内的进度计算
- 完成番茄后主页“今日统计”未即时刷新
- 切换主题后已打开的历史 / 设置弹窗仍显示旧配色；现在切换主题会关闭这些弹窗
- 托盘菜单回调在非主线程调用 Tk，存在偶发崩溃风险；改为主线程队列轮询
- 悬浮球只按主屏定位，多显示器下可能出现在非预期屏幕；现按光标所在显示器的工作区定位

### 变更
- 每次启动任务默认重新开始，不再继承上次的任务
- 历史记录默认只显示今天，更早日期通过“显示更多”展开
- 计时过程中不再每 200ms 重建按钮 / 分段控件图像，降低 CPU 占用
- 弹窗改为单例，快捷键（空格 / R / S / H / Esc）在弹窗打开时不再穿透到主窗口

## [2.3.0] - 2026-09-18

### 新增
- 启动动画：主窗口淡入 + 环形进度扫过，可在设置中关闭
- 安装包（Inno Setup，中文向导）与便携 zip 两种分发方式
- `tools/build_release.ps1` 一键构建：打包 + 便携 zip + 安装包

### 变更
- 打包由单文件（onefile）改为文件夹（onedir）：启动时不再解压到临时目录，启动速度显著提升
- 关闭 UPX 压缩，并排除 setuptools 等无用模块，减小体积、降低杀软误报

### 修复
- 双击桌面 / 开始菜单快捷方式会重复启动多个实例；现加入单实例保护，重复启动时激活已有窗口

### 说明
- 未做代码签名：首次运行可能被 SmartScreen 拦截，点「更多信息 → 仍要运行」即可

## [2.2.0] - 2026-09-13

### 新增
- 悬浮球增加环形进度动画：外圈轨道 + 进度弧，随时间推进，颜色随阶段变化
- 新增 MIT 许可证

## [2.1.0] - 2026-09-13

### 新增
- 历史记录面板：按天汇总番茄数与专注时长、任务明细，支持单条删除与一键清空
- 今日统计（完成数 / 专注时长），完成番茄时实时更新并持久化
- 任务标签，完成后按任务计入统计；任务按天隔离，跨天自动清空（计时中除外）
- 设置持久化（时长、主题、窗口位置、任务等），重启恢复
- 自动循环 + 长休息，可配置长休息间隔与自动开始
- 提示音与右下角轻量浮层通知
- 键盘快捷键：空格开始/暂停、R 重置、S 设置、H 历史、Esc 收起为小球
- 系统托盘支持（pystray）
- 深色 / 浅色主题切换
- 自绘应用图标（`tools/make_icon.py`），托盘与 exe 同源

### 变更
- 工程化：拆分为 `pomodoro` 包与 `ui` 子包，计时逻辑抽为可测试的 `TimerCore`
- 界面现代化：无边框圆角卡片、环形进度、分段控件、自绘圆角按钮与开关
- 仅保留 `main.py` 作为入口
- 打包配置改为 `main.py`，并写入 exe 版本信息

### 修复
- 高 DPI（如 150% 缩放）下字体模糊：声明 DPI 感知并按比例缩放界面
- 圆角与控件锯齿：Pillow 超采样抗锯齿；悬浮球改用逐像素 alpha 分层窗口
- 浅色主题圆角黑边、深色主题在浅色背景下的圆角暗边

## [2.0.0] - 2026-09-13

### 变更
- 由单文件脚本重构为工程化项目结构（`pomodoro` 包 + `ui` 子包 + 单元测试）
- 新增 `requirements.txt` 与打包配置

## 早期版本

- 单文件 `番茄钟.py`：倒计时 / 正计时、设置、小球悬浮窗、窗口拖动等基础功能。
